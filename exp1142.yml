model:
  model_type: "lvm_med"
  ann_type: "label"
  ensemble_size: 5
  gpus: "cuda:0"
  base_original_checkpoint: "lvm-med-resnet"
  val_epoch: 5
policy:
  policy_type: ranked
  seed: 2
  resume: True
  ensemble_kwargs:
    skip: False
    inf_test: True
  rounds: [[.01, 0], [.01, 0], [.01, 0], [.01, 0], [.01, 0]]
model_uncertainty:
  model_uncertainty_type: stochastic
  score_func: entropy_w_label_probs
  seed: 2
exp_dir: "LVMMED_sup_exp2_stochastic_v18"