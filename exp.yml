model:
  model_type: "dmpls"
  ann_type: "scribble"
  ensemble_size: 3
  gpus: "0"
policy:
  policy_type: random
  exp_dir: "DMPLS_test_run"
  seed: 0
  ensemble_kwargs:
    skip: False
  uncertainty_kwargs:
    skip: False
  rounds: [[.05, 0], [.05, 0], [.05, 0]]
model_uncertainty:
  model_uncertainty_type: no