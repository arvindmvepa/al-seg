model:
  model_type: "spml_mv"
  ann_type: "liger"
  ensemble_size: 2
  gpus: "0"
  sem_occ_concentration: 10
  sem_ann_loss_weight: .65
  sem_occ_loss_weight: .4
policy:
  policy_type: random
  seed: 0
  ensemble_kwargs:
    skip: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[1.0, 0]]
model_uncertainty:
  model_uncertainty_type: pass
exp_dir: "liger_new_params"