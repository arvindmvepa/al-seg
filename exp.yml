model:
  model_type: "db_dmpls"
  ann_type: "scribble"
  ensemble_size: 1
  gpus: "cuda:0"
  db_score_func: "pass_probs"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    train: False
  uncertainty_kwargs:
    skip: False
    delete_preds: False
  rounds: [[.001, 0], [.001, 0], [.001, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: entropy_w_label_probs
exp_dir: "db_dpmls_w_entropy_test_run"