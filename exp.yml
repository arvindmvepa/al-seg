model:
  model_type: "spml"
  ann_type: "box"
  ensemble_size: 3
model_uncertainty:
  model_uncertainty_type: "ensemble"
policy:
  policy_type: ranked
  exp_dir: "OO_full_refactor_ranked_v2"
  seed: 0
  ensemble_kwargs:
    cuda_devices: "2"
    skip: False
  uncertainty_kwargs:
    skip: False
  rounds: [[.05, 0], [.05, 0], [.05, 0]]