model:
  model_type: "db_dmpls"
  ann_type: "scribble"
  ensemble_size: 1
  gpus: "cuda:0"
  db_score_func: "entropy_score"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.001, 0], [.001, 0], [.001, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: mean_score
exp_dir: "db_dpmls_w_entropy_test_run"