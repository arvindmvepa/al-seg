model:
  model_type: "dmpls_s2l"
  ann_type: "scribble"
  ensemble_size: 5
  gpus: "cuda:0"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    inf_test: True
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.20, 0], [.20, 0], [.20, 0], [.20, 0], [.20, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: entropy_w_label_probs
exp_dir: "DMPS_S2L_exp_ensemble_entropy"