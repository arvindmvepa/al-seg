model:
  model_type: "strong"
  ann_type: "label"
  dataset: "LVSC"
  ensemble_size: 1
  max_iterations: 30000
  gpus: "cuda:0"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    inf_test: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.1, 0], [.05, 0], [.05, 0], [.05, 0], [.05, 0]]
data_geometry:
  data_geometry_type: kcenter_greedy
  dataset_type: "LVSC"
exp_dir: "strong_lvsc_coreset_test_run"