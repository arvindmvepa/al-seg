model:
  model_type: "lvm_med"
  ann_type: "label"
  dataset: "CHAOS_CT"
  ensemble_size: 5
  gpus: "cuda:0"
  base_original_checkpoint: "lvm-med-resnet"
  val_epoch: 5
policy:
  policy_type: random
  seed: 1
  resume: True
  ensemble_kwargs:
    skip: False
    inf_test: True
  rounds: [[.01, 0], [.01, 0], [.01, 0], [.01, 0], [.01, 0]]
exp_dir: "LVMMED_sup_CHAOS_exp1_random_v18"