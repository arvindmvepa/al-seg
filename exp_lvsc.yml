model:
  model_type: "strong"
  ann_type: "label"
  dataset: "LVSC"
  ensemble_size: 1
  max_iterations: 30000
  gpus: "cuda:0"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    inf_test: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.1, 0], [.05, 0], [.05, 0], [.05, 0], [.05, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: entropy_w_label_probs
exp_dir: "strong_w_coreset_chaos_test_run"