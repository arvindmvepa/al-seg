model:
  model_type: "dmpls"
  ann_type: "scribble"
  ensemble_size: 3
  max_iterations: 600
  gpus: "mps"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.1, 0], [.1, 0], [.1, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  scoring_method: ensemble_variance_ratio
exp_dir: "DMPLS_w_varRatio_test_run"