model:
  model_type: "strong"
  ann_type: "label"
  ensemble_size: 1
  max_iterations: 10
  gpus: "cuda:0"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    inf_test: False
    inf_train: True
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.01, 0], [.01, 0]]
data_geometry:
  data_geometry_type: kcenter_greedy
  alg_string: gpu_kcenter_greedy
  uncertainty_wt: 1.0
  use_uncertainty: True
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: entropy_w_label_probs
exp_dir: "strong_gpu_coreset_test_run"