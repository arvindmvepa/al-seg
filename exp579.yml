model:
  model_type: "db_strong"
  ann_type: "label"
  dataset: "DAVIS"
  ensemble_size: 3
  in_chns: 3
  max_iterations: 30000
  gpus: "cuda:0"
  db_score_func: "bald_score"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
    inf_test: True
  rounds: [[.1, 0], [.1, 0], [.1, 0], [.1, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: mean_score
exp_dir: "DMPLS_sup_DAVIS_exp0_db_bald_v14"