model:
  model_type: "strong"
  ann_type: "label"
  dataset: "CHAOS_CT"
  ensemble_size: 5
  gpus: "cuda:0"
  inf_train_type: "features"
policy:
  policy_type: ranked
  seed: 1
  ensemble_kwargs:
    skip: False
    inf_test: True
  rounds: [[.01, 0], [.01, 0], [.01, 0], [.01, 0], [.01, 0], [0.05, 0], [0.05, 0], [0.05, 0], [0.2, 0]]
data_geometry:
  data_geometry_type: coregcn
  dataset_type: "CHAOS_CT"
  seed: 1
  use_model_features: True
  starting_sample: 10
exp_dir: "DMPLS_sup_CHAOS_exp1_coregcn_v17"