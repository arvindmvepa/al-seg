model:
  model_type: "dmpls"
  ann_type: "scribble"
  ensemble_size: 1
  gpus: "cuda:0"
  inf_train_type: "features"
policy:
  policy_type: ranked
  seed: 2
  ensemble_kwargs:
    skip: False
    inf_test: True
  rounds: [[.05, 0], [.05, 0], [.05, 0], [.05, 0], [.20, 0]]
data_geometry:
  data_geometry_type: kcenter_greedy
  use_model_features: True
  seed: 2
exp_dir: "DMPLS_exp2_coreset_mdl_ft_v3"