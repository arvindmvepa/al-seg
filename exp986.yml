model:
  model_type: "strong"
  ann_type: "label"
  dataset: "DAVIS"
  ensemble_size: 5
  gpus: "cuda:0"
  in_chns: 3
policy:
  policy_type: ranked
  seed: 2
  ensemble_kwargs:
    skip: False
    inf_test: True
    inf_train: True
  rounds: [[.1, 0], [.1, 0], [.1, 0], [.1, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  score_func: ensemble_variance_ratio
exp_dir: "DMPLS_sup_DAVIS_exp2_var_v17"