model:
  model_type: "spml_sm"
  ann_type: "box"
  ensemble_size: 3
  gpus: "1"
policy:
  policy_type: ranked
  seed: 0
  ensemble_kwargs:
    skip: False
  uncertainty_kwargs:
    skip: False
    delete_preds: True
  rounds: [[.001, 0], [.001, 0], [.001, 0]]
model_uncertainty:
  model_uncertainty_type: ensemble
  scoring_method: entropy_w_label_probs
exp_dir: "spml_w_entropy_test_run1"